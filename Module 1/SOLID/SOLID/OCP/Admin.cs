namespace SOLID.OCP
{
    public class Admin
    {
        void Add(string id)
        {
            if (id.Contains("T"))
            {
                //add a new teacher
            }
            else
            {
                //add a new student
            }
            
            //if we want to add a course,
            //we have to add an "if else" statement and modify the code.
        }
    }
}